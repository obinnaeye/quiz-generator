from libs.model import UpdateQuizHistoryModel
from typing import List

def update_quiz_history (user_id: str, update: List[UpdateQuizHistoryModel]):
    #should update the quiz history object in the database with the userId and the update
    return
